// We must centralize workflow defaults so renderer and block builders stay in sync.
export const DEFAULT_WORKFLOW_NAME = 'Deploy to Cloudflare Workers'
export const DEFAULT_WORKFLOW_PATH = '.github/workflows/deploy.yml'
export const DEFAULT_NODE_VERSION = '24'
export const DEFAULT_PACKAGE_MANAGER = 'npm'
