// We must centralize defaults so renderer and normalizer cannot drift on fallback behavior.
export const DEFAULT_APP_NAME = 'my-nuxt-app'
export const DEFAULT_COMPATIBILITY_DATE = '2025-12-01'
export const DEFAULT_ROUTE = 'app.example.com'
